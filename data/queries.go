package data

// File contains SQL queries needed

var (
	// Select statements
	Select_all_Entity string = `SELECT * FROM Entity`

	Select_all_Sprite string = `SELECT * FROM Sprite`

	Select_all_Player string = `SELECT * FROM Player`
)
